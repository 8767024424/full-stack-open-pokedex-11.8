// src/utils.js

// The implementation of brokenFunction is updated to return 2, 
// matching the expectation of the test suite.
const brokenFunction = () => {
  return 2;
};

module.exports = brokenFunction;

// NOTE: The example provided in the prompt is also correct:
/*
module.exports = function brokenFunction() {
  return 2;
};
*/